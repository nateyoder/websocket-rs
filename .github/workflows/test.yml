name: Test

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
      - '.github/workflows/**'
  pull_request:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
      - '.github/workflows/**'

jobs:
  test:
    name: Test Python ${{ matrix.python-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm, windows-latest, macos-latest]
        python-version: ['3.12', '3.13', '3.14']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: |
            **/pyproject.toml
            **/Cargo.toml

      - name: Set up Python with uv
        run: |
          uv python install ${{ matrix.python-version }}
          uv venv --python ${{ matrix.python-version }}

      - name: Install dependencies with uv
        # uvloop has no Windows wheels; without it the uvloop half of the
        # peer-close matrix silently drops, and uvloop+wss is a combination
        # that actually regressed once.
        # aiofastnet is the transport wss:// defaults to when installed; the
        # TLS backend tests skip its cells without it.
        run: uv pip install maturin pytest pytest-asyncio websockets "uvloop; sys_platform != 'win32'" aiofastnet

      - name: Build
        run: uv run maturin develop --release

      - name: Run tests
        run: uv run pytest tests/

      - name: Run clippy
        run: cargo clippy -- -D warnings

      # The rustls transport is off by default, so nothing above compiles it.
      - name: Run clippy (rustls-transport)
        run: cargo clippy --features rustls-transport -- -D warnings

      # Unit tests link libpython (extension-module off); point the loader at
      # the interpreter's lib dir the same way pr.yml does.
      - name: Rust unit tests
        shell: bash
        run: |
          export PYO3_PYTHON="$(which python3)"
          export LD_LIBRARY_PATH="$(python3 -c 'import sysconfig; print(sysconfig.get_config_var("LIBDIR"))')${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
          # dyld ignores LD_LIBRARY_PATH; give macOS the same search path.
          export DYLD_FALLBACK_LIBRARY_PATH="$LD_LIBRARY_PATH"
          cargo test --lib --no-default-features
          cargo test --lib --no-default-features --features rustls-transport

      - name: Run rustfmt
        run: cargo fmt -- --check